# C-plus-plus
C++相关基础知识

- c++的new和delete，以及对应c语言中的内存分配和删除。
- *的用法：(1)定义指针类型，例如int * p = nullptr; (2)解引用/取值运算，int a = *p，就是取指针p对应的值。
- &的用法：（1）定义引用类型，int & p = a; p是a的别名。（2）取地址，int * p = & a; 取a变量的地址赋值给p指针。
- 引用常用于函数参数类型，（1）目的是不用拷贝。（2）需要修改原值使用左值引用，不需要修改原值使用常值引用const。
